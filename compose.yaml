services:
  analysis:
    build:
      context: .
    command:
      - python
      - main.py
      - --input
      - data/financials.csv
      - --summary-output
      - output/summary.json
      - --chart-output
      - output/charts.svg
    volumes:
      - ./output:/app/output
